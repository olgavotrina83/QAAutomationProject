# MCP Usage

Use this skill when working with MCP servers or agent integrations.

## Rules
- Document MCP servers in `docs/mcp.md`.
- Keep MCP secrets and tokens in local environment variables.
- Prefer read-only MCP tools for investigation before making repository changes.
- Add new vendor adapters instead of duplicating shared project rules.
